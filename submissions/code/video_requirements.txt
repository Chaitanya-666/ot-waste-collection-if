# Video Creation Requirements
# Install these packages to create optimization videos

# Core video/animation packages
matplotlib>=3.5.0          # For plotting and animation
numpy>=1.21.0              # For numerical operations
imageio>=2.9.0             # For video/GIF creation
Pillow>=8.3.0              # For image processing

# Optional but recommended for MP4 creation
ffmpeg-python>=0.2.0       # For MP4 video creation (requires ffmpeg installed)

# Alternative video creation (if ffmpeg not available)
moviepy>=1.0.3             # Advanced video editing capabilities

# For the ALNS integration
pathlib                  # Usually included in Python 3.4+
json                     # Built-in
typing                   # Built-in
datetime                 # Built-in
